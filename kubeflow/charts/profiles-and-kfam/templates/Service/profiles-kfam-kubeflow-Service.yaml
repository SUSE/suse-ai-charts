apiVersion: v1
kind: Service
metadata:
  labels:
    kustomize.component: profiles
  name: profiles-kfam
  namespace: kubeflow
spec:
  ports:
  - port: {{ .Values.service.port }}
  selector:
    kustomize.component: profiles
