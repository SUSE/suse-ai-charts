apiVersion: v1
data:
  ISTIO_GATEWAY: {{ .Values.configMapData.ISTIO_GATEWAY | quote }}
  ISTIO_HOST: {{ .Values.configMapData.ISTIO_HOST | quote }}
  RWO_PVC_SCHEDULING: {{ .Values.configMapData.RWO_PVC_SCHEDULING | quote }}
  TENSORBOARD_IMAGE: "{{- $registry := include "tensorboard-controller.suseImageRegistry" (dict "ctx" . "registry" .Values.configMapData.tensorboardImage.registry) -}}{{- if $registry }}{{ $registry }}/{{ end }}{{ .Values.configMapData.tensorboardImage.repository }}:{{ .Values.configMapData.tensorboardImage.tag }}"
kind: ConfigMap
metadata:
  name: tensorboard-controller-config-b98cb9gk9k
  namespace: kubeflow
