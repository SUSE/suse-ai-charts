apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: dex
  namespace: kubeflow
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dex
  action: ALLOW
  rules:
  - to:
    - operation:
        paths:
        - /dex/*
        - /dex
