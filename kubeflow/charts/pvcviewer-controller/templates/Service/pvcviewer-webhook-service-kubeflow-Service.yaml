apiVersion: v1
kind: Service
metadata:
  name: pvcviewer-webhook-service
  namespace: kubeflow
  labels:
    app: pvcviewer
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app: pvcviewer
    control-plane: controller-manager
