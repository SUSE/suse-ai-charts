{{/*
Chart-level prerequisite validations. This file renders nothing but fails fast
with a human-readable error when required dependencies are misconfigured.
*/}}
{{- if and .Values.modelRegistry.enabled (not .Values.pipelines.enabled) }}
{{- fail "modelRegistry.enabled=true requires pipelines.enabled=true (model-registry-server shares KFP's MariaDB and mysql-secret)" }}
{{- end }}
